# ( x1 x2 u -- )
sub pass   {
   tor dup2 equals if
      insertstring "PASS "
   else
      insertstring "FAIL "
   endif 
   insertstring "test #" rfrom dot
   insertstring ": returned " over dot
   insertstring ", expected " dot
   cr
}

sub accumulate { push 10 mul plus }
