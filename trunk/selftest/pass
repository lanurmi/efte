# ( x1 x2 u -- )
sub pass   {
   tor dup2 equals if
      insertstring "PASS "
   else
      insertstring "FAIL "
   endif 
   rfrom insertstring "test #" dot
   swap  insertstring ": returned " dot
   insertstring ", expected " dot
   cr
}

sub accumulate { push 10 mul plus }
