# ( x1 x2 u -- )
sub pass   {
   insertstring "test #" dot
   insertstring ": returned " over dot
   insertstring ", expected " dup dot
   equals if
      insertstring "PASS"
   else
      insertstring "FAIL"
   endif 
   cr }


sub accumulate { push 10 mul plus }
